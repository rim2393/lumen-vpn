# Security hardening TODO (API backend)

## В работе

- [x] Изолировать `SUPPORT` от админских привилегий управления пользователями (снять `USER_MANAGE`).
- [x] Закрыть `admin_compat` от `SUPPORT` и `SUBSCRIPTION_READ`.
- [x] Зафиксировать выдачу API-ключей на уровне фактических прав вызывающего в Remna-совместимом `/api/tokens`.
- [x] Добавить принудительные проверки на изменение/удаление пользователя и массовые действия в сервисном слое.
- [ ] Прогнать регрессионные проверки (контроль маршрутов доступа и эскалации).
- [ ] Зафиксировать изменения в CHANGELOG/notes при необходимости релиза.
