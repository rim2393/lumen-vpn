# Status

Public installer/docs scaffold is initialized for the Lumen VPN production
candidate path. It is ready for static validation and dry-run review. Real
installation requires private images, signed release manifests, and registry
access.

